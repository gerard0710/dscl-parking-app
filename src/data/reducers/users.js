import {
  ADD_USER,
  REMOVE_USER,
  UPDATE_USER,
  REMOVE_USERS,
  UPDATE_USERS,
  GET_USER,
  GET_USERS
} from '../constants/users'

const init = () => {
  return {
    users: {}
  }
}

const reducer = (state, action) => {}

export default { init, reducer }
