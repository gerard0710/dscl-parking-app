import {
  ADD_SLOT,
  REMOVE_SLOT,
  REMOVE_SLOTS,
  UPDATE_SLOT
} from '../constants/slots'

const init = () => {
  return {
    slots: {}
  }
}

const reducer = (state, action) => {}

export default { init, reducer }
